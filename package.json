{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "yarn workspace api start",
    "dev": "yarn workspace api start & yarn workspace app start",
    "build:web": "yarn workspace app build"
  }
}
